import React from 'react';
import { Box } from '@mui/material';

import './ExamplePageStyles.scss';

export const ExamplePage = () => {
  return (
    <Box className={'example-page'}>
      <div>An example page</div>
    </Box>
  );
};
